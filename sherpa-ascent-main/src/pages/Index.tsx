import { Game } from "@/components/game/Game";

const Index = () => {
  return (
    <div className="min-h-screen w-full flex items-center justify-center bg-gradient-to-b from-sky-top to-sky-bottom">
      <Game />
    </div>
  );
};

export default Index;
